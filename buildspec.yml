version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 rm --recursive "s3://${ARTIFACTS_BUCKET}/" 
artifacts:
  files:
    - '**/*'
  discard-paths: yes
  base-directory: 'dist*'